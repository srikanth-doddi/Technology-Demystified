// Runs after each page load when navigation.instant is enabled
document$.subscribe(function () {
  console.log("Initialize third-party libraries here");
  // e.g., initGlightbox(); or any other library bootstrapping
});
